import type { Metadata, Viewport } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Josh App - Personal Productivity Hub",
  description: "Your comprehensive personal productivity and creativity hub with AI assistance, task management, habit tracking, and more.",
  keywords: ["productivity", "task management", "habits", "AI", "personal dashboard"],
  authors: [{ name: "Josh" }],
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full">
      <body className={`${inter.className} h-full antialiased`}>
        {children}
      </body>
    </html>
  )
}